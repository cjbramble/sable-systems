[project]
name = "support-local-judge"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = ["deepeval==4.2.2"]

[dependency-groups]
dev = ["pytest==9.1.1"]

[tool.uv]
package = false

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = ["--import-mode=importlib"]
